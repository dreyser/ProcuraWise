# ProcuraWise — Backlog

Épicas E1-E11 mapeadas a las 29 fases del plan aprobado. Cada fase ≈ 1 sesión de Claude Code. P0 = requerido para el MVP; P1 = valioso pero no bloquea el cierre de su bloque. Dependencias son entre fases, no entre historias dentro de la misma fase (esas son secuenciales por definición).

Estado por fase se actualiza en [`docs/development/current-phase.md`](current-phase.md) (fase activa) y en la columna "Estado" de este documento conforme se completan sesiones. Estado inicial de todas las fases: **Not Started**.

---

## MVP (Fases 0-28)

### E1 — Fundación técnica (Bloque 0)

**Nota de IDs (2026-07-27):** las antiguas Fases 1 ("`identity`") y 2 ("Auth local") se restructuran para reflejar que el vertical slice se construye con un `DevelopmentIdentityProvider`, no con auth productiva. Los IDs `VS-2A`/`VS-2B`/`VS-2C`/`AUTH-PROD` reemplazan la numeración contradictoria; los números de fase ya cerrados u operativos (`0`, `8`-`28`) no se renumeran. Ver el plan de planeación correspondiente para el detalle completo de este cambio.

| Fase/ID | Historia | P | Depende de | Criterio de aceptación (resumen) | Estado |
|---|---|---|---|---|---|
| 0 | Bootstrap: docker-compose (mongo+azurite+redis+mailhog), `pyproject.toml`+uv, FastAPI `/health`, React hello-world, pre-commit, CI lint+test skeleton | P0 | — | `docker compose up` levanta todo; CI verde en PR vacío; `/health` responde 200 | ✅ Completed (2026-07-18, ejecutada como 3 sesiones — 1A/1B/1C, ver `current-phase.md`; pre-commit se movió fuera de esta fase — ver nota en VS-2A abajo, no forma parte de su alcance; "CI verde en PR vacío" verificado localmente, verificación en GitHub real pendiente del founder) |
| **VS-2A** (antes "Fase 1 — `identity`") | Tenant/User/Membership/VendorOrganization + `TenantCollection` (reglas estrictas: fuerza/rechaza `tenant_id` en insert, rechaza colisión de filtro y mutación de `tenant_id` vía `$set`/`$unset`/reemplazo, sin passthrough del driver) + `DevelopmentIdentityProvider` (`X-Dev-Membership-Id`, solo `environment=local\|test`) + `make seed-dev`/`seed-reset`. **Pre-commit explícitamente fuera de alcance** (CI ya cubre lint/format/typecheck) | P0 | 0 | Crear tenant+usuario+membership vía seed; `GET /api/v1/me` resuelve el actor correcto; una Membership de tenant A no lee recursos de tenant B (404); dev identity rechazada con `environment=production`; `make seed-dev` idempotente | 🔄 Implementado y verificado sin Docker (lint/typecheck/unit tests en verde, `make contracts` sin diff); **pendiente verificación de `make test-integration` contra Mongo real** (no había Docker disponible en la sesión de implementación) — mismo patrón de verificación en dos rondas que se usó en la Fase 1B, ver "Próximos pasos" en `current-phase.md` |
| **AUTH-PROD** (antes "Fase 2 — Auth local") | Auth local (email+password) + OIDC Microsoft/Google (authlib) + JWT propio + login/logout en frontend, sustituyendo a `DevelopmentIdentityProvider` **para rutas de comprador**. Excluye MFA. **Alcance acotado explícitamente** (decisión del founder, 2026-07-29): `vendor_contact` se queda en `DevelopmentIdentityProvider` hasta Fase 15; sin self-signup; sin recuperación de contraseña; JWT en memoria sin refresh token | P0 | VS-2C | Login exitoso ambos flujos; JWT contiene `tenant_id` correcto; sesión expira | ✅ Completed — implementado y verificado con Docker real (2026-07-29): `make test`/`test-integration`/`test-e2e`/`contracts`(x2) en verde, sin comitear — ver `docs/development/current-phase.md`. Verificación manual de OIDC contra Microsoft/Google reales pendiente (no bloqueante, IdP real requiere apps de prueba registradas) |

### E2 — Vertical slice RFP básico (Bloque 1) — cierra en VS-2C con el primer demo end-to-end

| Fase/ID | Historia | P | Depende de | Criterio de aceptación (resumen) | Estado |
|---|---|---|---|---|---|
| **VS-2B** (antes Fases 3-6) | Backend: `evaluations`+`requirements` (embebidos), `proposals`+`answers` (Proposal **es** la asociación evaluation↔vendor — no existe colección `evaluation_vendors`, corregido durante la planeación de VS-2B) con snapshot inmutable al enviar, `scoring` 0-5. Máquina de estados explícita (requirements/vendors solo en `draft`, answers solo en `collecting_responses`+`draft`, scores solo en `evaluating`, snapshot como fuente de verdad del cálculo). Mass assignment: `extra="forbid"` en todo schema de escritura. Router de proveedor físicamente separado bajo `/api/v1/vendor-portal/*`, sin acceso a `GET /api/v1/evaluations/{id}` | P0 | VS-2A | Flujo owner→requirement→vendor→proposal→submit→score→result funcional vía API; snapshot inmutable; proveedor no invitado→404; `vendor_contact` nunca accede a rutas de comprador; campo prohibido en body→422 | ✅ Completed — implementado, verificado con Docker real, comiteado y fusionado a `main` (2026-07-27, PR #14) |
| **VS-2C** (antes Fase 7 + frontend de 3-6) | Frontend: selector de Membership dev, páginas por rol, cliente OpenAPI regenerado (`client: 'react-query'`), Playwright E2E (spec de flujo desde recepción hasta completar + negativo de aislamiento — ver nota de cobertura en `current-phase.md`). Incluyó 2 ajustes de backend indispensables resueltos en la misma fase: `GET /api/v1/vendor-organizations` (catálogo de proveedores, paginado por cursor) y validación de `PATCH Requirement` sobre el documento resultante. **→ Cierre del vertical slice** | P0 | VS-2B | Demo manual de los 13 pasos desde la UI; Playwright verde; loading/empty/error visibles; actor activo siempre visible | ✅ **Completed y cerrado formalmente** (2026-07-28, verificación de cierre 2026-07-29) — `make test`/`test-integration`/`test-e2e`/`contracts`(x2) en verde, diff contra `origin/main` confirmado dentro del alcance autorizado, pendiente de commit — ver `current-phase.md` |

### E3 — Colaboración interna y auditoría (Bloque 2, parte 1)

| Fase | Historia | P | Depende de | Criterio de aceptación (resumen) | Estado |
|---|---|---|---|---|---|
| 8 | `audit` AuditEvent append-only, instrumentado retroactivamente en VS-2A/VS-2B/VS-2C | P0 | VS-2C | Toda mutación relevante del vertical slice genera un `AuditEvent` consultable | ✅ Completed (2026-07-30) — implementado y verificado con Docker real: `make lint`/`typecheck`/`test`/`test-integration`/`contracts`(x2) en verde. Alcance: evaluations (9 acciones) + proposals (submit) + scoring (score/complete), 13 acciones en total; login/OIDC (AUTH-PROD) y autosave de `ProposalAnswer` explícitamente fuera de alcance por decisión del founder — ver `current-phase.md` |
| 9 | RBAC completo (todos los roles del §4 de la spec) + `Assignment` por sección | P0 | 8 | Usuario sin rol adecuado recibe 403 en acción restringida | ✅ Completed (2026-07-30) — implementado y verificado con Docker real: `make lint`/`typecheck`/`test`/`test-integration`/`test-e2e`/`contracts`(x2) en verde. Alcance aprobado por el founder: roles de comprador (8 de los 10 del §4) + `Assignment` por sección + esqueleto mínimo de `platform_admin`/`tenant_admin`; `Colaborador proveedor` explícitamente diferido a Fase 15, consola/UI de administración a Fase 25 — ver `current-phase.md` |

### E4 — Wizard y biblioteca de requerimientos (Bloque 2, parte 2)

| Fase | Historia | P | Depende de | Criterio de aceptación (resumen) | Estado |
|---|---|---|---|---|---|
| 10 | Wizard guiado estático (sin IA) + autosave | P0 | 9 | Flujo de creación de evaluación guiado paso a paso, sin pérdida de datos al recargar | ✅ Completed (2026-07-31) — implementado y verificado con Docker real: `make lint`/`typecheck`/`test`/`test-integration`/`test-e2e` en verde, `make contracts` sin diff (fase 100% frontend, cero cambios de backend por decisión del founder — ver `current-phase.md`) |
| 11 | Biblioteca de requerimientos (`KnowledgeTemplate`, plantillas estáticas, sin IA) | P1 | 9 | Plantilla aplicable a nueva evaluación, reduce alta manual | ✅ Completed (2026-07-31) — `make lint`/`typecheck`/`test`/`test-integration`/`test-e2e` en verde (106 unit backend + 103 unit frontend, 173 integración Docker, 7 E2E), `make contracts` sin diff. Ver detalle completo en `current-phase.md` |
| 12 | Aprobación interna + publicación con validaciones (pesos/fechas/aprobador) + snapshot inmutable | P0 | 10 | Publicación bloqueada si pesos no suman 100% o falta aprobador; snapshot generado | ✅ Completed (2026-07-31) — `make lint`/`typecheck`/`test`/`test-integration`/`test-e2e` en verde (96 unit backend + 96 unit frontend, 146 integración Docker, 6 E2E), `make contracts` sin diff. Ver detalle completo en `current-phase.md` |

### E5 — IA de generación y descubrimiento (Bloque 3, parte 1)

| Fase | Historia | P | Depende de | Criterio de aceptación (resumen) | Estado |
|---|---|---|---|---|---|
| 13 | Adaptador `AIProvider` real (Azure OpenAI/Foundry) para Descubrimiento+Generación usando solo `InternalKnowledgeProvider`; salida validada por schema; `AIExecution` con costo/modelo/prompt-version; contrato de job asíncrono/polling adaptativo | P0 | 12 | Generación produce requerimientos candidatos válidos por schema sin red externa; polling sigue el contrato de `ADR 0012` | ✅ Completed (2026-08-01) — implementado y verificado con Docker real: `make lint`/`make typecheck` en verde; backend 124 unit + 194 integración Docker (mongo/azurite) + 5 integración contra el emulador real de Azure Service Bus (`make test-integration-ai`); frontend 115 unit + 8 E2E (incl. la nueva); `make contracts` corrido dos veces seguidas → sin diff. Ver detalle completo en `current-phase.md` |
| 14 | `ResearchProvider` completo + `CuratedSourceProvider` + `FoundryWebSearchProvider` implementado pero **desactivado por defecto** (feature flag, P1 condicionado a aprobación legal); trazabilidad de fuentes | P1 (abstracción P0, activación de Foundry P1 condicionado) | 13 | Flag apagado por defecto en todo ambiente; activarlo requiere aprobación legal documentada, no solo config | ✅ Completed (2026-08-01) — `make lint`/`make typecheck` en verde; backend 147 unit + 208 integración/API/seguridad Docker (mongo/azurite); frontend 116 unit; `make contracts` sin diff residual. `FoundryWebSearchProvider` implementado (REST directo, fail-closed) y verificado con fakes determinísticos — no se usaron credenciales reales de Foundry en ningún test. Ver detalle completo en `current-phase.md` |

**Nota:** el "hecho" de este epic para el MVP **no requiere** búsqueda web en vivo funcionando — ver contradicción documentada en `docs/planning/approved-mvp-plan.md`, sección 5.

### E6 — Confianza del proveedor y documentos (Bloque 3, parte 2)

| Fase | Historia | P | Depende de | Criterio de aceptación (resumen) | Estado |
|---|---|---|---|---|---|
| 15 | NDA real + conflicto de interés real, ambos vía `Agreement` (tipo `nda`/`conflict_of_interest`, usuario/IP/fecha/versión) + colaboradores múltiples por proveedor | P0 | 14 | Proveedor no accede al formulario de respuesta sin aceptar ambos `Agreement` | ✅ Completed (2026-08-02) — implementado y verificado con Docker real: `make lint`/`make typecheck` en verde; backend 156 unit + 228 integración/API/seguridad Docker; frontend 116 unit; `make test-e2e` 9/9 specs (incl. 1 nuevo: `vendor-onboarding.spec.ts` end-to-end de alta+invitación+Agreements, más `vertical-slice.spec.ts`/`isolation.spec.ts` reescritos para login real de proveedor); `make contracts` sin diff. Ver detalle completo en `current-phase.md` |
| 16 | `documents`: subida vía Azurite, escaneo AV stub, versionado, URLs temporales | P0 | 15 | Archivo subido, versionado, URL expira tras tiempo configurado | ✅ Completed (2026-08-03) — implementado y verificado con Docker real: `make lint`/`make typecheck` en verde; backend 163 unit + 253 integración/API/seguridad Docker (mongo/azurite); frontend 133 unit; `make test-e2e` 10/10 specs (incl. 1 nuevo: `documents.spec.ts` — proveedor sube/reemplaza/descarga evidencia real vía Azurite, envía, y comprador la revisa de solo lectura); `make contracts` sin diff. Módulo nuevo `documents/` (modelo+repo+servicio+antivirus stub EICAR+router proveedor y comprador); `AzureBlobStorage.generate_download_url` (Service SAS vía account key); `ProposalSnapshot.document_ids` congela la evidencia al enviar. Ver detalle completo en `current-phase.md` |

### E7 — Q&A y evaluación asistida por IA (Bloque 4)

| Fase | Historia | P | Depende de | Criterio de aceptación (resumen) | Estado |
|---|---|---|---|---|---|
| 17 | `qna` preguntas ligadas/generales, publicación anonimizada/privada, notificaciones | P0 | 16 | Pregunta de proveedor visible a comprador; respuesta publicada según visibilidad configurada | ✅ Completed (2026-08-03) — implementado y verificado con Docker real: `make lint`/`make typecheck` en verde; backend 168 unit + 279 integración/API/seguridad Docker; frontend 150 unit; `make test-e2e` 11/11 specs (incl. 1 nuevo: `qna.spec.ts` — proveedor pregunta, dueño responde con visibilidad privada/publicada-anonimizada, segundo proveedor solo ve lo publicado sin fuga de identidad); `make contracts` sin diff. Módulo nuevo `qna/` (modelo+repo+servicio+router proveedor y comprador); visibilidad binaria (`private`/`published_anonymized`, sin tercer estado); notificaciones = in-app únicamente (reutiliza `AuditEvent` + `PollingController`/ADR 0012, sin canal de entrega real ni entidad `Notification` nueva). "Aclaraciones" sobre `ProposalAnswer` y "asignación" delegable diferidas (Fase 21/no planificado). Ver detalle completo en `current-phase.md` |
| 18 | Evaluación asistida por IA (riesgos/score sugerido) con "aceptar o modificar" obligatorio | P0 | 13, 17 | Score sugerido nunca se guarda sin acción explícita del evaluador humano | ✅ Completed (2026-08-03) — implementado y verificado con Docker real: `make lint`/`make typecheck` en verde; backend 174 unit + 299 integración/API/seguridad Docker + 6 Service Bus real (`make test-integration-ai`); frontend 154 unit; `make test-e2e` 12/12 specs (incl. 1 nuevo: `ai-score-suggestions.spec.ts`); `make contracts` sin diff. `AIExecution` extendido (nuevo `use_case="score_suggestion"`, `proposal_id`/`snapshot_id` opcionales); worker con dispatch real multi-topic (`ai-requirement-generation` + `ai-score-suggestion`); "aceptar o modificar" reutiliza el endpoint de score ya existente (`Score`/`ScoreWriteRequest` ganan `source_ai_execution_id` opcional, sin endpoint de escritura nuevo en `ai/` — la IA nunca escribe `Score` directamente); `ScoringPage.tsx` gana botón "Sugerir con IA" + bloque de sugerencia con "Usar esta sugerencia" (prellena, editable). ADR 0022 documenta la política de datos (contenido de `ProposalAnswer` autorizado, sin gate legal duro, decisión del founder). Ver detalle completo en `current-phase.md` |

### E8 — TCO y scoring económico (Bloque 5, parte 1)

| Fase | Historia | P | Depende de | Criterio de aceptación (resumen) | Estado |
|---|---|---|---|---|---|
| 19 | `tco` CostItem, cálculo 1-5 años, FX congelado desde `FXRate` (actualización manual por `platform_admin`) | P0 | 18 | TCO recalculado no cambia al actualizar `FXRate` después de publicación | ✅ Completed (2026-08-04) — implementado y verificado con Docker real: `make lint`/`make typecheck` en verde; backend 190 unit + 322 integración/API/seguridad Docker; frontend 161 unit; `make test-e2e` 13/13 specs (incl. 1 nuevo: `tco.spec.ts`); `make contracts` sin diff. Módulo nuevo `tco/` (modelo `CostItem`/`FXRate`/`FrozenFxRate`/`TcoResult`, `TcoService.calculate()` puro, `FXRateRepository`/`FXRateService`); `Evaluation` gana `base_currency`/`tco_horizon_years`; `CostItem` embebido en `Proposal.cost_items` (mismo patrón que `ProposalAnswer`); FX+costos+TCO se resuelven y congelan dentro del mismo `ProposalService.submit()` ya existente — nunca se recalcula después con una tasa nueva, prueba directa del criterio de aceptación en `test_proposal_submit_tco_freeze.py`. `FXRate` es create-only, gestionado vía `admin/router.py` sin UI dedicada (mismo precedente que `CuratedSource`, Fase 14). Fórmula de agregación por partida (única para los 3 tipos de costo) confirmada por el founder ante la ausencia de una fórmula documentada en ADRs/spec — `Decimal`/`Decimal128` en todo el flujo monetario (desviación deliberada del único precedente existente, `float` en `proposals`). Ver detalle completo en `current-phase.md` |
| 20 | Scoring económico completo: TCO normalizado 70%, condiciones comerciales 15% (sub-pesos por ADR 0009), riesgo/predictibilidad 15%; escala humana 0-5 con guías; scores extremos requieren comentario; criterios configurables antes de publicar, congelados al publicar | P0 | 19 | Fórmula final 40/20/40 + flags eliminatorios calcula correctamente contra casos de prueba fijos | ✅ Completed (2026-08-05) — implementado y verificado con Docker real: `make lint`/`make typecheck` en verde; backend 210 unit + 341 integración/API/seguridad Docker; frontend 170 unit; `make test-e2e` 13/13 specs (`vertical-slice.spec.ts` extendido); `make contracts` sin diff. Nueva entidad `EconomicAssessment` (10 `CriterionScore` comerciales/riesgo, no una extensión de `Score`); TCO normalizado nunca se persiste — calculado en vivo comparando `TcoResult.grand_total` (Fase 19) entre propuestas enviadas; fórmula final `40 x [0.70x(TCO_pct/100) + 0.15x(Comercial_pct/100) + 0.15x(Riesgo_pct/100)]` verificada contra casos fijos exactos (2 proveedores: TCO 100%/50% → económico 40.0/26.0 → final 100.0/86.0) en `test_economic_scoring_results.py`, incluyendo `mandatory_alert` (VS-2B/Fase 9, sin mecanismo nuevo) coexistiendo correctamente. Autorización reutiliza `Assignment`/`enforce_section_assignment` con sentinel fijo `section="economic"`; pesos de criterios editables solo por el owner en `draft`, fijos por lo demás (founder decision), congelados en `EvaluationSnapshot` al publicar. Ver detalle completo en `current-phase.md` |

### E9 — Negociación y decisión (Bloque 5, parte 2)

| Fase | Historia | P | Depende de | Criterio de aceptación (resumen) | Estado |
|---|---|---|---|---|---|
| 21 | Ronda final de negociación: Ronda 0 inicial (inmutable) + Ronda 1 opcional (BAFO), versionado `inherited`/`modified`/`removed` + `source_proposal_version`; invalidación de scores; recálculo completo de TCO por versión; comparación inicial vs. final | P0 | 20, 8 (auditoría) | Modificar una respuesta invalida su score; TCO nunca mezcla costos entre versiones; toda reapertura queda auditada con justificación | ✅ Completed (2026-08-05) — implementado y verificado con Docker real: `make lint`/`make typecheck` en verde; backend 219 unit + 355 integración/API/seguridad Docker; frontend 179 unit; `make test-e2e` 14/14 specs (incl. 1 nuevo: `proposal-negotiation.spec.ts`); `make contracts` sin diff. `Proposal` gana `round`/`snapshots` (reemplaza el slot único `snapshot`) en vez de una entidad `NegotiationRound` nueva o estados nuevos en `EvaluationStatus`/`ProposalStatus` — la reapertura reutiliza `collecting_responses`/`draft` ya existentes, reactivando documentos/Q&A/respuestas/costos sin tocar esos módulos. `Score` no cambia de grano (ya vivía por `snapshot_id` desde Fase 9) — la invalidación se resuelve en lectura (`_scores_for_current_snapshot`, fallback solo para respuestas `inherited` sin cambio), probada directamente contra casos fijos en `test_negotiation_round_scoring.py`. `EconomicAssessment` gana `snapshot_id` sin fallback (recalificación completa cada ronda). Nueva vista de comparación Ronda 0 vs Ronda 1 (`ProposalVersionComparisonPage.tsx`) sin endpoint de diff dedicado. Ver detalle completo en `current-phase.md` |
| 22 | `decisions`: vista de aprobador + memo de cierre | P0 | 21 | Decisión requiere aprobador humano explícito; nunca hay adjudicación automática | ✅ Completed (2026-08-06) — implementado y verificado con Docker real: `make lint`/`make typecheck` en verde; backend 227 unit + 374 integración/API/seguridad Docker; frontend 184 unit; `make test-e2e` 15/15 specs (`decision-approval.spec.ts` nuevo); `make contracts` sin diff. Nuevo bounded context `decisions/` (`Decision`/`DecisionSnapshot`, colecciones propias `decisions`/`decision_snapshots`), grain 1:1 con `Evaluation`, solo creable/editable con `Evaluation.status=="completed"`. `Decision.approver_membership_id` es un campo propio, nunca copiado de ni escrito hacia `Evaluation.approver_membership_id` (founder, resolviendo la única pregunta bloqueante del plan) — las dos aprobaciones son actos independientes reutilizando el mismo patrón de validación de Fase 12 (bloqueo de autoaprobación, rol `approver` exigido). Selección de proveedor validada server-side contra `Proposal` `submitted` de la propia evaluación; `selected_proposal_id`/`selected_proposal_snapshot_id` derivados siempre del `Proposal.current_snapshot`, nunca aceptados del cliente. `DecisionSnapshot` ("memo de cierre") copia `ScoringService.get_results()` en el instante de aprobar, mismo patrón insert-only/`snapshot_id` determinístico/"flip status luego snapshot" que `EvaluationSnapshot`. Sin ranking calculado ni persistido. Ver detalle completo en `current-phase.md` |

### E10 — Reportes y notificaciones (Bloque 5 cierre + Bloque 6 parte 1)

| Fase | Historia | P | Depende de | Criterio de aceptación (resumen) | Estado |
|---|---|---|---|---|---|
| 23 | Reportes/exports asíncronos vía worker (8 entregables de §10 de la spec), import Excel/CSV con preview+mapeo | P0 | 22 | Cada reporte se genera como job asíncrono y sigue el contrato de polling | ✅ Completed (2026-08-06) — implementado y verificado con Docker real: `make lint`/`make typecheck` en verde; backend 255 unit + 392 integración/API/seguridad Docker (incluye 9 tests de aislamiento negativo dedicados); frontend 191 unit; `make test-e2e` 17/17 specs (`report-generation.spec.ts`/`requirements-import.spec.ts` nuevos); `make contracts` sin diff. Nuevo bounded context `reports/` (`Report`, colección propia, grain 1:N insert-only por evaluación), mismo contrato de job asíncrono `queued/running/succeeded/failed` que `AIExecution` (ADR 0012). Librerías de generación resueltas por el founder (ADR 0023): `reportlab`+`openpyxl`+`python-docx`+`csv` stdlib. Representación intermedia genérica (`ReportDocument`/`ReportWorkbook`) desacopla el ensamblaje de datos (8 funciones puras) de los 4 renderers; persistencia en container de Blob propio con descarga SAS bajo demanda; readiness gateada por tipo (`decision_record` requiere `Decision.status=="approved"`). Import de Requirements (Excel/CSV, preview+mapeo) reutiliza `add_requirements_bulk` (Fase 11), restringido a `Evaluation.status=="draft"`. Ver detalle completo en `current-phase.md` |
| 24 | Notificaciones reales (Azure Communication Services) + centro in-app | P1 | 23 | Notificación real enviada en al menos un evento clave (invitación, publicación) | ✅ Completed (2026-08-07) — implementado y verificado con Docker real: `make lint`/`make typecheck` en verde; backend 260 unit + 407 integración/API/seguridad Docker (incluye 7 tests de aislamiento negativo dedicados, tenant + identidad); frontend 196 unit; `make test-e2e` 18/18 specs (`notifications.spec.ts` nuevo); `make contracts` sin diff. Nuevo bounded context `notifications/` (`Notification`, fila in-app síncrona best-effort + envío de email asíncrono vía worker, contrato `pending/sending/sent/exhausted`). Founder resolvió la pregunta bloqueante del plan por **Opción A**: los 8 de 9 eventos de la spec con punto de enganche síncrono existente quedan conectados (invitación, publicación, pregunta recibida, respuesta publicada, propuesta enviada, reapertura, aprobación pendiente ×2, cierre) — "Fecha próxima/vencida" queda diferido (requiere generalizar el nuevo `time_based_tasks` hacia un barrido con deduplicación, candidato a Bloque 6/hardening). Proveedor real `AzureCommunicationServicesEmailProvider` (ADR 0024) detrás del Protocol `NotificationEmailProvider`, con `LoggingNotificationEmailProvider` como sustituto local/CI (cierra también la apertura de ADR 0020 sobre Mailhog/Mailpit). Reintentos con backoff fijo vía nuevo parámetro `time_based_tasks` en `shared/worker_loop.py`. Autorización por identidad (no rol) en el repositorio — un miembro del mismo tenant nunca puede leer/marcar como leída la notificación de otro. Centro in-app: campana+dropdown en `AppShell` (poll no-terminal, mismo patrón que `useQnaPolling.ts`), sin página `/notifications` dedicada en esta fase. Ver detalle completo en `current-phase.md` |

### E11 — Billing, hardening y lanzamiento (Bloque 6 cierre)

| Fase | Historia | P | Depende de | Criterio de aceptación (resumen) | Estado |
|---|---|---|---|---|---|
| 25 | Billing/Admin básico P1: Stripe checkout, consola admin cross-tenant auditada | P1 | 9 | Cobro de prueba exitoso en modo sandbox; acción admin cross-tenant queda auditada con motivo | ✅ Completed (2026-08-08) — implementado y verificado con Docker real + E2E: `make lint`/`typecheck`/`test`/`test-integration`/`test-e2e`/`contracts`(x2) en verde. Alcance aprobado por el founder: compra única por-evaluación vía Checkout hospedado (sin suscripción, sin enforcement de entitlements); consola `platform_admin` mínima de solo lectura (evaluaciones + compras cross-tenant, reutilizando el esqueleto de Fase 9). Mitad automatizable del criterio (wiring de checkout/webhook/idempotencia) probada sin red vía `LocalPaymentProvider`; el cobro de prueba real en Stripe Test Mode queda pendiente de demo manual del founder (procedimiento en `docs/operations/deployment.md`) — ver `current-phase.md` |
| 26 | Hardening: rate limiting, CSRF, headers, escaneo secretos/deps en CI, WCAG AA, performance, backup/restore, `threat-model.md` cerrado | P0 | 24 | Escaneo de seguridad en CI sin hallazgos críticos abiertos; restore de backup probado | ✅ Completed (2026-08-09) — implementado y verificado con Docker real + E2E: `make lint`/`typecheck`/`test`/`test-integration`/`test-e2e`/`contracts`(x2) en verde; `pip-audit`/`pnpm audit` bloqueantes en high/critical con 0 hallazgos abiertos; backup/restore verificado a nivel MVP (`make backup-demo`, Atlas M0 no soporta backups gestionados); `k6` p95=127ms, 0% error rate. `threat-model.md` cerrado formalmente. Ver `current-phase.md` |
| 27 | Infra Azure real (Bicep) + CI/CD GitHub Actions OIDC staging→prod | P0 | 26 | Deploy a staging exitoso vía pipeline, sin secretos de larga vida en el repo | ✅ Completed (2026-08-16) — implementado 2026-08-10, primer deploy real ejecutado por el founder el 2026-08-14, validación operacional dedicada (2026-08-14 a 2026-08-16) confirmó el pipeline real (`deploy-staging.yml` vía `workflow_dispatch`) produciendo una revisión sana y validada end-to-end (API, MongoDB Atlas, Storage, Azure OpenAI, Service Bus/worker, Stripe Test Mode) tras corregir 9 defectos reales — ver `current-phase.md` |
| 28 | UAT piloto 1-3 empresas, fixes, lanzamiento controlado | P0 | 27 | Al menos 1 evaluación completa cerrada end-to-end por un piloto real | 🔄 Bloque A cerrado y confirmado con evidencia real (deploy sano, login OIDC real por navegador). Bloque B en curso (1 tenant piloto con owner/aprobador/evaluadores provisionados). Bloque C en curso: defectos reales #1/#2/#3/#5/#6/#7 corregidos y confirmados/verificados vía uso real; proveedor respondió una propuesta completa, calificación en progreso; criterio de aceptación todavía no cumplido — ver `current-phase.md` |

### E12 — Remediación UAT piloto (post-Fase 28, no numerado en el plan/roadmap original)

**Origen (2026-08-24):** el primer UAT real end-to-end de una evaluación completa (creación, requerimientos, aprobación, onboarding/respuesta de proveedor, scoring, TCO/económico, reapertura/negociación, resultados/decisión, notificaciones, múltiples proveedores) expuso 20 hallazgos reales (UAT-01 a UAT-20), analizados contra el repositorio real y aprobados por el founder junto con 9 decisiones de producto (A-I). No estaba contemplado en `roadmap.md`/`mvp-scope.md` originales — es trabajo nuevo descubierto por el piloto, no una reinterpretación del alcance ya cerrado del MVP. Detalle completo del análisis (root cause, clasificación, impacto por hallazgo) vive en el historial de conversación de la sesión de análisis; este backlog registra solo la estructura y el estado.

**Pregunta bloqueante resuelta (Opción 1, aprobada):** `ApprovalStatus` se mantiene sin cambios (`not_requested|pending|approved|rejected`, sin un 5º valor `changes_requested`). "Solicitar cambios" (R2) es una acción de UI que persiste `rejected`, preserva comentarios por requerimiento, y su evento de auditoría debe distinguir explícitamente una solicitud de cambios de un rechazo genérico (actor, timestamp, evaluación, requerimiento(s) afectados, comentario, y el reenvío subsecuente) — nunca colapsar ambos en un evento indistinguible.

**UAT-12 (sin salida entre login comprador/proveedor) ya está cerrado** (PR #62, mergeado a `main`) — no reabrir en ningún bloque de este remediation.

| Fase/ID | Historia | P | Depende de | Criterio de aceptación (resumen) | Estado |
|---|---|---|---|---|---|
| R1A | UAT-01 (Q&A rompe navegación) + UAT-11 (mensaje de conflicto/estado distinguible por causa real, no genérico) | P0 | 28 | Q&A vive dentro del shell de navegación de la evaluación; el mensaje de error distingue conflicto de versión real vs. evaluación no en recepción vs. propuesta no editable | ✅ Completed (2026-08-25) — implementado y verificado localmente: `make lint`/`typecheck`/`test`/`test-e2e`(20/20)/`contracts` en verde, 100% frontend, redeploy real pendiente — ver `current-phase.md` |
| R1B | UAT-16 (reopen multi-proveedor independiente) + UAT-14 (ocultar Comercial/Riesgo a evaluador functional/technical) | P0 | 28 | 3 proveedores en rondas distintas simultáneas sin bloqueo cruzado; evaluator_functional/technical nunca ve la sección económica | Not Started |
| R1C | UAT-10 (ampliar cobertura negativa de aislamiento de proveedor) | P0 | 28 | `isolation.spec.ts` cubre explícitamente scoring/economic-assessment/decisions para `vendor_contact` | Not Started |
| R2 | UAT-06/07/08: Reviewer + aprobación en dos pasos (Owner→Reviewer→Approver) + navegación contextual derivada de rol/assignment | P0 | R1A, R1B, R1C; **requiere ADR 0026 antes de implementar** | Journey completo demostrado end-to-end con 3 actores reales (owner/reviewer/approver); reviewer nunca aprueba, approver nunca edita (test de autorización negativo) | Not Started — bloqueado por ADR 0026 |
| R3 | UAT-04/09 (progreso/estado consolidado por rol) + UAT-15 (autosave de scoring, reutilizando `AnswerAutosaveController`) | P1 | R2 | Owner ve estado consolidado real (bloqueadores, próxima acción, completitud por evaluador); calificar ya no requiere guardado manual por fila | Not Started |
| R4 | UAT-02 (pesos %), UAT-03 (Company Profile), UAT-05 (layout tabla requerimientos), UAT-13 (deep-link notificaciones), UAT-17 (Evaluación Comercial unificada), UAT-18 (guías por criterio económico), UAT-19 (cantidad de sugerencias de IA), UAT-20 (escape de ruta obsoleta) | P1/P2 | 28 (sin dependencia fuerte entre sí ni con R1/R2/R3) | Ver criterios individuales por hallazgo en el análisis de la sesión — mejoras de producto MVP restantes | Not Started |

**Gate de piloto externo:** no se considera listo hasta que R1 y R2 estén completos, con toda la verificación automatizada en verde, desplegados a staging, y validados manualmente en staging con actores distintos (Owner → Reviewer → Approver → Owner publica/invita). R3 y R4 son mejoras deseables pero no forman parte de este gate salvo que un hallazgo futuro cambie su prioridad.

---

## Notas explícitas de alcance del backlog

- **La integración de IA (E5) no es requisito para completar el primer vertical slice (E2, VS-2A/VS-2B/VS-2C).** El vertical slice usa scoring 100% manual, sin `AIProvider`.
- **Stripe, correo real y Azure productivo no son requisito de la primera fase (E1) ni del vertical slice (E2).** Stripe llega en la Fase 25 (P1), correo real en la Fase 24, Azure real en la Fase 27 — todas muy posteriores al cierre del vertical slice.
- **Auth productiva (`AUTH-PROD`) no es requisito para completar el vertical slice.** El vertical slice usa `DevelopmentIdentityProvider` (`X-Dev-Membership-Id`), gateado a `environment in (local, test)` — ver VS-2A.
- Cada fase, al cerrar sesión, actualiza `docs/development/current-phase.md` y `docs/development/session-handoff.md` — único mecanismo de continuidad entre sesiones sin memoria compartida.
