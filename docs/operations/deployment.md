# ProcuraWise — Despliegue

Este documento describe el diseño de despliegue aprobado. **Fase 27 (2026-08-10) implementó el IaC (Bicep, `/infra`), los Dockerfiles y los workflows de deploy (`deploy-staging.yml`/`deploy-prod.yml`) — verificados localmente (`az bicep build`, `docker build`, smoke test real contra Mongo/Azurite locales) pero aún no ejecutados contra una suscripción Azure/organización MongoDB Atlas reales.** El primer deploy real es una acción manual pendiente del founder, documentada en [`infra/scripts/bootstrap-oidc.md`](../../infra/scripts/bootstrap-oidc.md) (plan de Fase 27, Pregunta bloqueante 2, Opción A — mismo mecanismo de cierre "en código, deploy real pendiente" ya usado en Fases 15/25). Hasta que ese runbook se ejecute, todo el desarrollo sigue corriendo 100% local vía Docker Compose.

## Ambientes

| Ambiente | Infraestructura | Cuándo existe | Propósito |
|---|---|---|---|
| Local | Docker Compose: Mongo, Azurite por defecto; perfil opcional `servicebus` (emulador de Azure Service Bus + SQL Server, Fase 13) vía `make dev-up-servicebus`. Cola: `InMemoryMessageBus` en proceso por defecto, `ServiceBusMessageBus` contra el emulador si `queue_backend=service_bus` (ver [ADR 0020](../architecture/decisions/0020-composicion-servicios-desarrollo-local.md) y [ADR 0021](../architecture/decisions/0021-ai-provider-abstraction.md)) | Desde Fase 1B (perfil `servicebus` desde Fase 13) | Desarrollo con datos sintéticos, sin Azure real |
| Development | CI (GitHub Actions), recursos económicos | Desde Fase 1C (`.github/workflows/`; despliegue real Fase 27) | Validación automatizada en cada PR |
| Staging | Azure real, similar a producción (`ENVIRONMENT=staging`, ver más abajo) | IaC lista desde Fase 27; recursos vivos pendientes del deploy real del founder | E2E, UAT antes del piloto |
| Production | Azure Container Apps, aprobaciones, backups, alertas, mínimo privilegio | IaC lista desde Fase 27; recursos vivos pendientes de Fase 28 | Piloto (Fase 28) y operación real |

**`ENVIRONMENT=staging`** (Fase 27, `Settings.environment`, `shared/config.py`) es un valor propio, no una reutilización de `production` — comparte los validadores fail-closed de producción (`Settings.is_production_like`: OIDC/Azure OpenAI/notificaciones-si-están-activas real, sin `queue_backend=memory`, sin JWT default) pero **nunca** acepta una clave Stripe live (`_reject_live_stripe_key_outside_production` solo exime a `environment=="production"` literalmente) — staging queda permanentemente limitado a `sk_test_`, evitando cobros reales durante UAT/smoke tests.

## Recursos Azure por ambiente (IaC implementado en Fase 27 — `infra/bicep/`, ver detalle por recurso abajo; recursos vivos pendientes del primer deploy real)

- **Azure Container Apps**: hosting de API y worker (ver [ADR 0019](../architecture/decisions/0019-azure-container-apps-hosting.md)).
- **MongoDB Atlas**: tier M0 (free) con IP allowlist para todo el MVP (ver [ADR 0015](../architecture/decisions/0015-tier-mongodb-atlas-m0.md)). Desde Fase 19, la colección `fx_rates` (tasas de cambio para TCO, gestionadas por `platform_admin`) es no tenant-scoped, mismo patrón que `curated_sources` (Fase 14) — create-only vía `POST /api/v1/admin/fx-rates`, sin UI de administración dedicada (ver [ADR 0008](../architecture/decisions/0008-fuente-fx-tco.md)). Desde Fase 20, la colección tenant-scoped `economic_assessments` (índice único `(tenant_id, evaluation_id, proposal_id)`, `migrations/0017_economic_assessments_indexes.py`) — sin infraestructura nueva, sin cola nueva (cálculo síncrono, mismo principio que Fase 19). Desde Fase 21, ese mismo índice único de `economic_assessments` se reemplaza para incluir `snapshot_id` (`migrations/0018_economic_assessments_snapshot_id_index.py`) — sin colecciones nuevas, la ronda de negociación vive como campos aditivos (`round`, `snapshots`) directamente en `proposals`.
- **Azure Blob Storage**: almacenamiento de documentos (Azurite como equivalente local).
- **Azure Service Bus**: cola de jobs asíncronos en staging/producción, adaptador real (`ServiceBusMessageBus`) implementado desde Fase 13 (`shared/messaging.py`, `queue_backend=service_bus` — ver [ADR 0005](../architecture/decisions/0005-worker-asincrono-service-bus.md), [ADR 0020](../architecture/decisions/0020-composicion-servicios-desarrollo-local.md) y [ADR 0021](../architecture/decisions/0021-ai-provider-abstraction.md)). El emulador oficial (`mcr.microsoft.com/azure-messaging/servicebus-emulator:2.0.1` + `mcr.microsoft.com/mssql/server:2022-CU26-ubuntu-22.04`, pinned) vive en `docker-compose.yml` bajo el perfil opcional `servicebus` (`make dev-up-servicebus` / `make test-integration-ai`) — no es requisito de `make dev`/`make test`/`make dev-up`, siguiendo la disciplina de ADR 0020 de no levantar infraestructura sin consumidor concreto en el arranque por defecto. **4 colas reales** (`infra/bicep/modules/service-bus.bicep`, Fase 27, namespace tier Standard): `ai-requirement-generation`/`ai-score-suggestion` (Fases 13/18) + `notification-email`/`report-generation` (Fases 23/24) — más que las 2 que `docker/servicebus-emulator/config.json` define, que quedó desactualizado desde esas fases (gap preexistente del emulador local, irrelevante mientras `queue_backend=memory` sea el default local; contra Azure real las 4 son obligatorias). El mismo worker (`worker/main.py`) despacha las 4.
- **Azure Key Vault**: gestión de secretos vía identidad administrada — **implementado en Fase 27** (`infra/bicep/modules/key-vault.bicep`), sin secretos en código ni en GitHub. `storage-connection-string`/`service-bus-connection-string` los genera el propio Bicep; `mongodb-uri`/`jwt-secret`/credenciales OIDC/`azure-openai-api-key`/`acs-connection-string`/credenciales Stripe se poblan manualmente (`infra/scripts/bootstrap-oidc.md`, paso 8) — `main.bicep` nunca los crea ni les escribe valor alguno, ni siquiera un placeholder (corregido tras la primera validación real de staging, ver "Defectos encontrados..." abajo).
- **Azure Container Registry**: **implementado en Fase 27** (`infra/bicep/modules/container-registry.bicep`, `adminUserEnabled: false` — pull/push solo vía identidad administrada/OIDC federado). Sin firma/escaneo de imágenes todavía — no solicitado por el criterio de aceptación textual de Fase 27, candidato a hardening posterior.
- **Azure Communication Services**: notificaciones reales (desde Fase 24).
- **Azure OpenAI / Foundry**: `AIProvider` implementado desde Fase 13 (`AzureOpenAIProvider`, sobre el SDK oficial `openai`). Config requerida en `staging`/`production` por igual desde Fase 27 (`Settings._require_real_ai_config_in_production`, vía `Settings.is_production_like`): `azure_openai_endpoint`, `azure_openai_api_key`, `azure_openai_deployment` — ninguno tiene default fuera de `local`/`test`, así que el despliegue falla explícitamente si falta alguno en vez de arrancar con IA silenciosamente deshabilitada. Desde Fase 18, el mismo `AzureOpenAIProvider` sirve también la evaluación asistida (`ai_score_suggestion_enabled: bool = True`, `shared/config.py`) — un booleano simple, sin validador de precondiciones fail-closed como el de Foundry (ver [ADR 0022](../architecture/decisions/0022-politica-datos-evaluacion-asistida-ia.md)).
- **Foundry Web Search** (`FoundryWebSearchProvider`, Fase 14, ADR 0011): adaptador implementado (REST directo vía `httpx` + `azure-identity` para el token Entra ID de `POST {endpoint}/openai/v1/responses` — sin SDK `azure-ai-projects`/`azure-ai-agents`), pero **desactivado en todo ambiente**, incluida producción, hasta aprobación legal documentada. Config: `FOUNDRY_WEB_SEARCH_ENABLED` (default `false`), `FOUNDRY_WEB_SEARCH_ENDPOINT`, `FOUNDRY_WEB_SEARCH_AGENT_NAME`, `FOUNDRY_WEB_SEARCH_API_VERSION` (pinned `v1`), `FOUNDRY_LEGAL_APPROVAL_REFERENCE`. Fail-closed en `Settings._require_foundry_preconditions_when_enabled`: si el flag es `true`, los otros tres valores (menos la API version, que siempre tiene default) deben estar presentes o el proceso no arranca — en cualquier ambiente, no solo producción. **Provisión de infraestructura pendiente y fuera del alcance de esta fase**: crear el proyecto Foundry, el recurso "Grounding with Bing Search" (recurso pago, requiere rol Contributor/Owner), la conexión del proyecto al recurso Bing, y el agente pre-provisionado (`FOUNDRY_WEB_SEARCH_AGENT_NAME`) con la tool `web_search` adjunta — son pasos de infraestructura/ops de una sola vez (vía `azure-ai-projects` SDK, CLI, o el portal Foundry), no algo que el adaptador haga en runtime; se documentan aquí cuando efectivamente se aprovisionen, cerca del piloto (Fase 28), no antes.

## Stripe (billing, Fase 25)

`PaymentProvider` (`service/procurawise/billing/provider.py`) sigue el mismo patrón de frontera que Azure OpenAI/ACS: `LocalPaymentProvider` (sin red) es el default en todo ambiente no-producción y en CI; `StripePaymentProvider` (`service/procurawise/billing/stripe_payment_provider.py`, único archivo autorizado a importar el SDK `stripe`, ver [ADR 0025](../architecture/decisions/0025-pagos-stripe-checkout-hospedado.md) y CLAUDE.md S5.1) solo se usa si `billing_enabled=true` y la configuración de Stripe está completa. `billing_enabled=false` por defecto — el feature se puede desplegar a producción apagado.

**Config** (`shared/config.py`): `billing_enabled`, `stripe_secret_key`, `stripe_webhook_secret`, `stripe_price_id_evaluation`, `stripe_request_timeout_seconds`, `billing_webhook_event_retention_days`. `stripe_secret_key`/`stripe_webhook_secret` van a Key Vault en producción (mismo mecanismo que `acs_connection_string`); `stripe_price_id_evaluation` no es secreto pero sí específico de ambiente. Fuera de `environment=production`, el validador de arranque rechaza cualquier `stripe_secret_key` que no empiece con `sk_test_` — una clave `sk_live_...` nunca puede terminar en un `.env` de desarrollo por accidente.

**Webhook**: endpoint público `POST /api/v1/billing/stripe/webhook`, sin JWT (la firma HMAC es la única autenticación), `include_in_schema=False`. Nunca se agrega a `/health/ready` — un incidente del lado de Stripe no debe tumbar el readiness probe del servicio.

### Provisionar una cuenta Stripe de prueba (Test Mode)

Prerequisito para el criterio de aceptación de la fase ("cobro de prueba exitoso en modo sandbox"). Ningún paso de esta sección toca dinero real ni requiere una cuenta Stripe verificada para cobros reales — Test Mode no necesita verificación de negocio.

1. Crear (o reutilizar) una cuenta en <https://dashboard.stripe.com>, confirmar que el toggle **Test mode** está activo (esquina superior derecha del dashboard).
2. **API Keys**: Developers → API keys → copiar la *Secret key* (`sk_test_...`) → exportar como `STRIPE_SECRET_KEY`.
3. **Product + Price**: Product catalog → Add product (p. ej. "Evaluación ProcuraWise") → agregar un Price de tipo *One time* (no recurring — este diseño es `mode="payment"`, sin suscripción) → copiar el Price ID (`price_...`) → configurar como `stripe_price_id_evaluation` (o exportar como `STRIPE_PRICE_ID_EVALUATION` para el test opt-in, ver abajo).
4. **Webhook secret** (dos opciones, elegir una):
   - **Local vía Stripe CLI** (recomendado para la demo manual): `stripe login`, luego `stripe listen --forward-to localhost:8000/api/v1/billing/stripe/webhook` — el CLI imprime un `whsec_...` de sesión, exportarlo como `STRIPE_WEBHOOK_SECRET` antes de levantar la API.
   - **Dashboard**: Developers → Webhooks → Add endpoint, URL pública, eventos `checkout.session.completed` y `checkout.session.expired` → copiar el *Signing secret* resultante.

### Demo manual del flujo completo (Checkout + webhook)

1. Exportar `billing_enabled=true`, `STRIPE_SECRET_KEY`, `STRIPE_WEBHOOK_SECRET`, `stripe_price_id_evaluation` y levantar la API (`make dev` o `uv run uvicorn ...`).
2. En otra terminal: `stripe listen --forward-to localhost:8000/api/v1/billing/stripe/webhook` (deja corriendo).
3. Iniciar sesión como `tenant_admin`, ir a Facturación, pegar un `evaluation_id` real y pulsar "Pagar" — el navegador es redirigido al Checkout hospedado real de Stripe (no al simulador local, con `billing_enabled=true`).
4. Completar el pago con una [tarjeta de prueba de Stripe](https://stripe.com/docs/testing) (p. ej. `4242 4242 4242 4242`, cualquier fecha futura/CVC/código postal).
5. Confirmar en la terminal del Stripe CLI que `checkout.session.completed` se reenvió con `200`, y en el navegador que la página de éxito muestra "Pago confirmado" tras el polling.
6. Verificar en Mongo (`purchases`) que el `Purchase` correspondiente quedó `status="paid"` con `stripe_payment_intent_id`/`amount_total`/`currency` poblados, y en `audit_events` que se registró `billing_payment_succeeded`.
7. Registrar la evidencia (IDs de Stripe test-mode: `cs_test_...`/`pi_test_...`, e IDs internos: `purchase_id`) en [`docs/development/current-phase.md`](../development/current-phase.md) al cerrar la fase, junto con la fecha de la demo — mismo mecanismo que CLAUDE.md S7 exige para cualquier demo manual de cierre de fase.

CI nunca depende de esta cuenta: `tests/integration/test_stripe_sandbox_checkout.py` (marcador `stripe_sandbox`) crea una Sesión real (sin cobro) para verificación opcional del adaptador, pero se auto-salta sin `STRIPE_SECRET_KEY`/`STRIPE_PRICE_ID_EVALUATION` y `make test`/`make test-integration` lo excluyen explícitamente — solo corre con `pytest -m stripe_sandbox` invocado a mano.

## Pipeline CI/CD

- **IaC**: Bicep (ver [ADR 0004](../architecture/decisions/0004-bicep-vs-terraform.md)), `/infra/{bicep,params,scripts}` — **implementado en Fase 27** (`infra/bicep/main.bicep` + 8 módulos: Log Analytics, Container Registry, Key Vault, Container Apps Environment, Storage Account, Service Bus, identidad administrada, Container App genérico reusado para api/worker). Scope de resource group (`az deployment group create`), no de suscripción — el resource group en sí se crea una única vez, manualmente, durante el bootstrap (`infra/scripts/bootstrap-oidc.md`), para que la identidad OIDC del pipeline nunca necesite un permiso más amplio que `Contributor` sobre ese resource group ya existente (mínimo privilegio). Validado con `az bicep build`/`az bicep build-params` (sintaxis, sin errores) — sin ejecución real todavía (Pregunta bloqueante 2 del plan de Fase 27, Opción A).
- **CI/CD de despliegue**: `.github/workflows/deploy-staging.yml` (push a `main` o manual) / `deploy-prod.yml` (solo manual, gateado por el `environment: production` de GitHub — aprobación requerida, configurada en Settings del repo) — **implementados en Fase 27**. Ambos: build+push de `Dockerfile.api`/`Dockerfile.worker` a ACR, `az deployment group create` (Bicep), migraciones vía `az containerapp exec` contra el contenedor api recién desplegado (reutiliza la `Settings` real que Bicep ya inyectó, sin duplicar validación en el runner), health check contra `/health/ready`. Login a Azure vía `azure/login` con OIDC federado (`id-token: write`, sin secreto de larga vida) — identidades separadas por ambiente (`AZURE_CLIENT_ID` para staging, `AZURE_CLIENT_ID_PROD` para producción), ver `infra/scripts/bootstrap-oidc.md`.
- **CI/CD de calidad e integración** (implementado desde Fase 1C, 2026-07-18): `.github/workflows/ci.yml` (lint, typecheck, tests unitarios backend/frontend, build de producción del frontend, verificación de que el contrato OpenAPI/cliente TS generado no está desactualizado — ver [ADR 0007](../architecture/decisions/0007-contratos-openapi-orval.md)), `.github/workflows/integration.yml` (pruebas contra Mongo+Azurite reales), `.github/workflows/security.yml` (secret scanning con `gitleaks`, dependency scanning con `pip-audit`/`pnpm audit`). Reemplaza el boceto original `lint.yml`/`test.yml` — se consolidó en `ci.yml` con jobs separados por responsabilidad (`backend`/`frontend`/`contracts`) para no duplicar checkout/setup entre workflows disparados por el mismo evento, y se separó `integration.yml` para no acoplar la señal rápida de lint/typecheck al arranque de contenedores Docker. Ningún job usa secretos; todos corren con `permissions: contents: read`. Detalle completo del diseño y de las Actions pinneadas por SHA en `docs/development/session-handoff.md` (entrada de Fase 1C).
- **Registro de imágenes**: Azure Container Registry (`adminUserEnabled: false` — pull/push vía identidad administrada/OIDC federado, nunca usuario/contraseña de admin), imagen sin firmar/escanear todavía (candidato a hardening posterior si se necesita, no solicitado por el criterio de aceptación textual de Fase 27).

## Gestión de secretos

Azure Key Vault vía identidad administrada — **implementado en Fase 27** (`infra/bicep/modules/key-vault.bicep`, RBAC-based, sin access policies clásicas). Ningún secreto se comitea al repositorio ni se configura como GitHub Secret de larga vida — la autenticación de CI/CD a Azure usa OIDC federado (`AZURE_CLIENT_ID`/`AZURE_TENANT_ID`/`AZURE_SUBSCRIPTION_ID` son identificadores públicos, no credenciales). Cada Container App (api/worker) tiene su propia identidad administrada asignada por el usuario con rol `Key Vault Secrets User` (solo lectura de secretos, nunca escritura) acotado a ese Key Vault — los valores reales (connection string de MongoDB Atlas, `JWT_SECRET`, credenciales OIDC/Azure OpenAI/ACS/Stripe) se poblan una sola vez, manualmente, por el founder (`infra/scripts/bootstrap-oidc.md`, paso 8, tras un deploy standalone de Key Vault en el paso 6 y la asignación de `Key Vault Secrets Officer` al founder en el paso 7); `storage-connection-string`/`service-bus-connection-string` los genera el propio template Bicep automáticamente (Bicep ya conoce esos valores vía `listKeys()`, no hay razón para pedírselos al founder). `main.bicep` nunca escribe ningún valor sobre los 8 secretos del founder — un redeploy ordinario nunca los toca (ver "Defectos encontrados..." abajo).

## Migraciones de base de datos (diseño aprobado)

Carpeta `migrations/` numerada + colección de control `_migrations` + `indexes.py` por módulo como fuente de verdad. **No se ejecutan automáticamente al arrancar el proceso** (`python -m procurawise.shared.migrations` es un comando aparte, `make migrate` en local) — en staging/producción (Fase 27), `deploy-staging.yml`/`deploy-prod.yml` las ejecutan como un paso explícito del propio workflow, después de desplegar la nueva revisión y antes del health check final.

## Rollback

**Implementado en Fase 27** — Container Apps mantiene cada revisión desplegada; un revert es activar la revisión anterior, sin rebuild ni nuevo deploy:

```bash
az containerapp revision list --resource-group rg-procurawise-<env> --name procurawise-api-<env> -o table
az containerapp revision activate --resource-group rg-procurawise-<env> --name procurawise-api-<env> --revision <revision-name-anterior>
```

Mismo mecanismo para `procurawise-worker-<env>`. `deploy-prod.yml` imprime este comando exacto como sugerencia (`Rollback hint on failure`) si el health check post-deploy falla. Sin pérdida de datos por diseño: un rollback de Container Apps solo cambia qué imagen/revisión sirve tráfico, nunca toca Mongo Atlas — si el deploy que falló ya corrió una migración incompatible hacia adelante, revertir la revisión de la app **no** revierte esa migración (las migraciones de este proyecto son aditivas por convención, ver `migrations/`, así que una revisión anterior del código sigue funcionando contra un esquema con campos nuevos que simplemente ignora).

## Backup / restore

**Estado: verificación de nivel MVP ejecutada (Fase 26, 2026-08-09).** Atlas M0 (el tier aprobado para todo el MVP, [ADR 0015](../architecture/decisions/0015-tier-mongodb-atlas-m0.md)) **no ofrece backups gestionados** — esa capacidad requiere un tier M10+, un upgrade explícitamente clasificado en ese mismo ADR como "post-MVP, sin gatillo numérico predefinido", que esta fase no reabre. Tampoco existe infraestructura real hasta la Fase 27. La verificación de esta fase queda por lo tanto acotada deliberadamente a lo demostrable hoy: un ciclo real `mongodump`→`mongorestore` contra el Mongo local de Docker Compose, **no equivalente a un backup gestionado de Atlas** — decisión del founder (plan de Fase 26, pregunta bloqueante #1, Opción A).

**Script**: `scripts/backup_restore_demo.sh` (invocado vía `make backup-demo`) —
1. Cuenta documentos por colección en `procurawise_local` (estado "antes").
2. `mongodump` de `procurawise_local` dentro del propio contenedor `procurawise-mongo` (la imagen oficial de Mongo ya incluye `mongodump`/`mongorestore`, sin instalar tooling adicional en el host).
3. `mongorestore` hacia una base de verificación separada (`procurawise_backup_verify`, vía `--nsFrom`/`--nsTo`) dentro del mismo `mongod` — evita levantar un segundo contenedor solo para la demo.
4. Vuelve a contar documentos por colección en la base restaurada (estado "después") y compara exactamente contra el conteo "antes".
5. Limpieza: borra la base de verificación y el dump temporal dentro del contenedor.

**Runbook operativo** (una vez exista Mongo Atlas real, Fase 27+): mismo patrón de comandos (`mongodump`/`mongorestore`) apuntando a la URI de Atlas en vez del contenedor local — Atlas M10+ además añade snapshots automáticos gestionados por el proveedor, que este script no sustituye ni intenta reproducir, solo complementa como verificación manual adicional.

**Ejecución real de esta demo** (2026-08-09, `make backup-demo`): 26 colecciones (`_migrations`, `agreements`, `ai_executions`, `assignments`, `audit_events`, `billing_accounts`, `curated_sources`, `decision_snapshots`, `decisions`, `documents`, `economic_assessments`, `evaluation_snapshots`, `evaluations`, `knowledge_templates`, `memberships`, `notifications`, `platform_admins`, `proposals`, `purchases`, `qna_questions`, `reports`, `scores`, `tenants`, `users`, `vendor_invitations`, `vendor_organizations`), 9025 documentos totales — conteo por colección idéntico entre `procurawise_local` (antes) y `procurawise_backup_verify` restaurada (después), verificado por el propio script. **Bug real encontrado y corregido durante la construcción del script**: `mongorestore --dir` apuntando directamente al subdirectorio nombrado por la base de datos (`.../procurawise_local`, en vez del directorio padre del dump) fallaba silenciosamente — cada archivo `.bson` se reportaba con "don't know what to do with file ..., skipping" y la base de verificación quedaba vacía sin ningún error fatal (el script sí detectó el fallo correctamente, comparando conteos "antes" vs. "después" — 0 documentos restaurados no coincide con ninguno de los 26 conteos originales). Corregido apuntando `--dir` al directorio padre, dejando que `mongorestore` descubra el subdirectorio `procurawise_local` él mismo junto con `--nsFrom`/`--nsTo`.

## Performance (k6, Fase 26)

**Estado: ejecutado (2026-08-09).** Verifica NFR-003 (50 usuarios concurrentes, global de la plataforma, no por-tenant — `approved-mvp-plan.md` §4.13) con evidencia real, no solo diseño aprobado. Script: `scripts/perf/rfp-read-load.js` (k6) — autentica una sola vez en `setup()` (nunca por VU/iteración: martillar `/auth/login` habría disparado de inmediato el rate limiter agregado en esta misma fase, `rate_limit_login_max_attempts=5/60s` por IP, y el throughput del propio login nunca fue el objetivo de NFR-003 — tiene su propio límite bajo intencional como control de seguridad, no de performance), luego ejercita el endpoint de lectura síncrono más transitado por un comprador autenticado (`GET /api/v1/evaluations`) con 50 VUs sostenidos. Deliberadamente sin escritura (crear evaluación, guardar respuesta de propuesta) en esta primera pasada — requeriría datos desechables por-corrida (invitación de proveedor, evaluación en borrador) que no es seguro fabricar 50-anchos contra la base compartida de `make seed-dev` sin contaminar las fixtures de cualquier otra sesión de desarrollo; candidato a un escenario más elaborado cuando exista una historia de seed/datos desechables por corrida (Fase 27+).

**Hallazgo real durante la construcción del script**: `GET /api/v1/me` iba a ser un segundo endpoint objetivo, pero devuelve 401 contra un JWT real de comprador — sigue cableado al mecanismo de identidad pre-AUTH-PROD (`identity.dev_provider.get_current_context`, header `X-Dev-Membership-Id`), nunca migrado al JWT real que usa el resto de rutas de comprador. Descubierto por el propio script de k6, no por los tests existentes. Corregirlo queda fuera del alcance de esta fase (rate limiting/CSRF/headers/WCAG/performance/backup) — el script final solo usa `GET /api/v1/evaluations`; ver `docs/security/threat-model.md` para el registro completo del hallazgo.

**Cómo correrlo**:
```
make dev-up && make seed-dev
(cd service && uv run uvicorn procurawise.api.main:app --port 8000) &
k6 run scripts/perf/rfp-read-load.js
```

**Umbral**: p95 < 500ms en los endpoints síncronos objetivo, tasa de error < 1% — endpoints de IA/reportes explícitamente excluidos (ya son asíncronos vía job, sin SLA síncrono aplicable). Sin gate en CI en esta fase (no solicitado por el criterio de aceptación textual del backlog) — ejecución manual documentada aquí.

**Resultado de la corrida real** (2026-08-09, 50 VUs pico, `stages: 20s→50/40s@50/10s→0`): `http_req_duration` p95=127.46ms (umbral: <500ms, holgura amplia), `http_req_failed`=0.00% (umbral: <1%), 2610 iteraciones completas, 0 interrumpidas, 2613/2613 checks exitosos. Ambos umbrales en verde. Detalle completo, incluida la corrida fallida inicial que expuso el hallazgo de `/me` arriba (antes de corregir el script), en [`docs/development/current-phase.md`](../development/current-phase.md), entrada de Fase 26.

## Aprovisionamiento inicial (Fase 27)

**Estado: IaC/pipeline implementados y verificados localmente (2026-08-10); primer deploy real pendiente del founder.** Runbook completo (creación del resource group, App Registration + federated credential OIDC por ambiente, rol `Contributor` acotado al resource group, secrets del repositorio, población de Key Vault, cluster MongoDB Atlas manual, primer `az deployment group create`) en [`infra/scripts/bootstrap-oidc.md`](../../infra/scripts/bootstrap-oidc.md) — no se repite aquí para evitar que las dos copias diverjan; este documento solo referencia el resultado.

Verificación local realizada en esta sesión (sin suscripción Azure real, plan de Fase 27 Pregunta bloqueante 2 Opción A):
- `az bicep build`/`az bicep lint` sobre `infra/bicep/main.bicep` + los 8 módulos → sin errores ni warnings.
- `az bicep build-params` sobre `infra/params/{staging,production}.bicepparam` → sin errores.
- `docker build` de `service/Dockerfile.api`/`Dockerfile.worker` → exitoso.
- Smoke test real: ambas imágenes corridas vía `docker run` contra el Mongo/Azurite de `docker-compose.yml` (misma red `procurawise_default`) — `GET /health/live` → `200`; `GET /health/ready` → `200` con `mongodb`/`storage` en `true`; worker arranca, conecta a Mongo/Blob, y llega a `worker ready` despachando las 4 colas reales.
- `deploy-staging.yml`/`deploy-prod.yml` validados como YAML bien formado; no ejecutados contra Azure real (requieren los secrets del repositorio que solo el bootstrap manual puede proveer).

**Resultado del primer deploy real**: pendiente — se registrará aquí (fecha, `apiFqdn`, respuesta de `/health/ready`) cuando el founder ejecute el runbook, mismo mecanismo ya usado para las demos manuales de Fases 15 (OIDC)/25 (Stripe)/26 (backup/k6).

### Defectos encontrados en la primera validación real de staging (post-Fase 27) y su remediation

**2026-08-14.** El founder ejecutó por primera vez el runbook completo contra una suscripción Azure/organización MongoDB Atlas reales (no solo `az bicep build`/smoke test local). API, MongoDB Atlas, Azure Storage, Azure OpenAI (tras ajuste manual), Service Bus (4 colas, worker AMQP), Key Vault RBAC, y OIDC Microsoft/Google quedaron operativos. Esa validación expuso 5 defectos reales, corregidos en una sesión de remediation dedicada (rama `fix/staging-integration-remediation`), sin reabrir ninguna decisión arquitectónica:

1. **Bicep pisaba los secretos del founder en cada redeploy** — `main.bicep` escribía un valor placeholder sobre los 8 secretos del founder sin ninguna condición, no solo en el primer deploy. Corregido eliminando ese recurso por completo de `main.bicep`: estos secretos ahora se poblan exclusivamente vía el runbook, nunca por Bicep, bajo ninguna combinación de parámetros.
2. **API version de Azure OpenAI inválida** (`2026-01-01-preview`, 404 contra el recurso real) — corregida a `2024-10-21` (GA vigente, confirmado compatible con Structured Outputs) en `shared/config.py`, `infra/params/{staging,production}.bicepparam` y el fixture de test correspondiente.
3. **`bootstrap-oidc.md` no otorgaba el rol de Key Vault necesario** para que el founder pudiera poblar secretos (`Forbidden` real) — el runbook ahora incluye un paso explícito de asignación de `Key Vault Secrets Officer` acotado al vault, antes de poblar secretos. Esto además expuso un problema de secuencia más profundo: al dejar de crear Bicep los secretos (defecto 1), Key Vault debe existir *antes* del primer deploy completo — el runbook ahora incluye un deploy standalone del módulo de Key Vault como paso previo.
4. **`max_tokens` rechazado por el modelo Azure OpenAI real** (requiere `max_completion_tokens`) — corregido en `ai/azure_openai_provider.py`, un solo parámetro, sin flag de capability (ningún modelo adicional en alcance).
5. **Stripe rechazaba `timeout` como parámetro de Checkout Session** ("Received unknown parameter: timeout") — `stripe_payment_provider.py` migrado de la llamada clásica `stripe.checkout.Session.create(**kwargs)` a un `stripe.StripeClient` por instancia con `http_client=stripe.RequestsClient(timeout=...)`; el timeout de transporte ya nunca viaja como parámetro de la API.

**Estado**: código corregido, pruebas nuevas/actualizadas en verde, `make lint`/`make typecheck`/`make test`/`make test-integration`/`make contracts` en verde, `az bicep build`/`az bicep lint` en verde. **Revalidación live (redeploy real a staging con estos 5 fixes) queda pendiente** — se ejecuta en el chat operacional, no en esta sesión de código. Detalle completo (causa raíz, versiones exactas de SDK, diseño de cada fix) en el historial de la sesión de remediation; resultado del registro en [`current-phase.md`](../development/current-phase.md).

## Runbook

Runbook operativo completo pendiente de construir tras el primer deploy real (Fase 27-28) — este documento tendrá contenido especulativo mínimo hasta entonces. Las notas operativas ya existentes abajo (Fases 20-21) siguen aplicando sin cambios, son independientes de la infraestructura.

**Nota operativa (Fase 20)**: un componente económico con `status: "no_comparable"` en `GET .../results` (TCO de un proveedor en 0 o ausente) es el caso esperado más probable — típicamente un proveedor que aún no capturó ningún `CostItem` — no un error de la aplicación. No requiere intervención: el owner completa la evaluación económica del proveedor cuando este capture sus costos.

**Nota operativa (Fase 21)**: un proveedor con `proposal.round == 0` y `status == "submitted"` mientras la evaluación está en negociación (otras propuestas ya en `round == 1`) es el caso esperado de "no invitado a la Ronda 1" — su propuesta inicial sigue vigente para el comparativo final, no un error. Igualmente, `EconomicAssessment` ausente (`economic.status == "not_available"` en `GET .../results`) justo después de una reapertura es esperado incluso para una propuesta ya recalificada en Ronda 0 — el assessment económico no se hereda entre rondas por diseño (decisión #4 de la fase), siempre requiere una recaptura completa.

## Última prueba de backup/restore

**2026-08-09** (Fase 26, `make backup-demo`, nivel MVP contra Mongo local — no equivale a un backup gestionado de Atlas, ver sección "Backup / restore" arriba): 26 colecciones / 9025 documentos, conteo idéntico entre origen y restaurada. Próxima actualización de este campo: cuando exista infraestructura Atlas real (Fase 27+) y, si se decide un upgrade de tier post-MVP (ADR 0015), la primera verificación contra un backup gestionado real.

## Referencias

- [`docs/architecture/architecture.md`](../architecture/architecture.md), sección 7.
- [ADR 0004](../architecture/decisions/0004-bicep-vs-terraform.md), [ADR 0019](../architecture/decisions/0019-azure-container-apps-hosting.md).
- [`infra/scripts/bootstrap-oidc.md`](../../infra/scripts/bootstrap-oidc.md) — runbook completo de aprovisionamiento inicial (Fase 27).
